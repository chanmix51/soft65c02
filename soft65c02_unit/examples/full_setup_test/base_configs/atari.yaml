# this could also be "none" but allows for including atari include files which are guarded
target: atari
compiler: cc65

config_file: "./basic.cfg"

# setup files for a basic application that uses startup, and calls _main
src_files:
  - ./crt0.s
